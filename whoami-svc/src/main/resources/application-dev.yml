server:
  port: 8084

spring:
  jpa:
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: "*"

planner:
  account-service-endpoint: http://localhost:8081
  company-service-endpoint: http://localhost:8082