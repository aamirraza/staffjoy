# application.yml

spring:
  profiles:
    active: dev
  application:
    name: ical-service

planner:
  common:
    sentry-dsn: ${SENTRY_DSN}
    deploy-env: ${DEPLOY:V2}
  company-service-endpoint: http://company-service:8080